import type { Page } from '../../../types';

// Patterns is currently a placeholder section. Pattern pages can be
// added here in future, keyed by their route path. Each value is a `Page`.
const patterns: Record<string, Page> = {};

export default patterns;
