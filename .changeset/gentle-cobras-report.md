---
'@braid-design-system/icons': minor
---

Add `@braid-design-system/icons` as the canonical SVG package. Assets live under `svg/` (unsuffixed default, plus `.ios` / `.android` when a platform drawing differs), including `tag.svg` and Share (`share.svg`, `share.ios.svg`, `share.android.svg`).
