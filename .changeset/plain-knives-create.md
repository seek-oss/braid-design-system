---
'braid-design-system': major
---

Replace dev-time assertion library `assert` with `tiny-invariant`

### MIGRATION GUIDE:

Ensure you are on at least version [12.6.0 of `sku`][sku] - this is the minimum version that supports stripping assertions from the `tiny-invariant` library.

[sku]: https://github.com/seek-oss/sku/releases/tag/sku%4012.6.0
