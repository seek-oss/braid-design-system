import '../applyReset';
import treatTheme from './theme.treat';
import { Theme } from '../theme';

const theme: Theme = {
  name: 'jobStreet',
  background: '#e5e5e5',
  treatTheme,
};

export default theme;
