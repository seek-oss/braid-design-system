import { makeTokens } from '../baseTokens/seekAsia';

const blue = '#0c4b85';

export default makeTokens({
  name: 'jobsDb',
  bodyBackground: '#eee',
  brand: blue,
  formAccent: blue,
  brandAccent: '#ff9000',
});
