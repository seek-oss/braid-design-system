import { ComponentDocs } from '../../../../site/src/types';
import examplesForIcon from '../../private/examplesForIcon';
import { IconPromote } from './IconPromote';

const docs: ComponentDocs = {
  migrationGuide: true,
  examples: examplesForIcon(IconPromote),
};

export default docs;
