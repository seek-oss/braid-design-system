import { style } from 'sku/treat';

export const highlightBar = style({ left: 0, top: 0, bottom: 0 });

export const toneBorder = style({
  opacity: 0.3,
});

export const cautionBorder = style({
  opacity: 0.6,
});
