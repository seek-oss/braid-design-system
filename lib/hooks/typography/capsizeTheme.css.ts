import { createThemeContract } from '@vanilla-extract/css';

export const metricVars = createThemeContract({
  ascent: null,
  descent: null,
  unitsPerEm: null,
  capHeight: null,
  lineGap: null,
});
