import { vars } from '../themes/vars.css';

export const outlineTransition = 'outline-color .125s ease';
export const resetOutline = `${vars.focusRingSize} solid transparent`;
export const focusOutlineColor = vars.borderColor.focus;
