import type { CrackleConfig } from '@crackle/core';

const braidConfig: CrackleConfig = {
  pageRoots: ['src', 'site-crackle/src'],
  appShell: 'site-crackle/src/App.tsx',
};

export default braidConfig;
