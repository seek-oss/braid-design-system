import { ReactPortal } from 'react';
interface AnnouncementProps {
    children: string | undefined | null;
}
export declare const Announcement: ({ children }: AnnouncementProps) => ReactPortal | null;
export {};
