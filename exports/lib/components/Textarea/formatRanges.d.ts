import { ReactChild } from 'react';
import { TextareaProps } from './Textarea';
export declare const formatRanges: (
  value: string,
  highlightRanges: TextareaProps['highlightRanges'],
) => ReactChild[];
